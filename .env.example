# Secret used to sign/verify login session tokens (JWT). Generate one with:
#   openssl rand -base64 48
# Set this in your Vercel project's Environment Variables — do not commit the real value.
JWT_SECRET=
